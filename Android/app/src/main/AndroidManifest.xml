<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.vteam.vmangaandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.SEARCH" />

                <meta-data
                    android:name="android.app.searchable"
                    android:resource="@xml/searchable" />
            </intent-filter>
        </activity>
        <activity android:name=".MangaDetailActivity" />
        <activity android:name=".MangaReadActivity" />

        <provider
            android:name=".data.MangaProvider"
            android:authorities="info.vteam.vmangaandroid"
            android:exported="false" />

        <activity android:name=".MangaFavoriteActivity"></activity>
    </application>

</manifest>